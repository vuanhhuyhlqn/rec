# Ablation: MIP only (masked item prediction).
_base_: ../base.yaml
run_name: pretrain_mip_only
pretrain:
  tau: 0.1
  tasks: [mip]
  training: {lr: 0.0001, epochs: 10, batch_size: 32, log_every: 50, save_every: 1, num_workers: 4}
