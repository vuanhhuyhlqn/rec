# Ablation: MAP only (masked attribute prediction).
_base_: ../base.yaml
run_name: pretrain_map_only
pretrain:
  tau: 0.1
  tasks: [map]
  training: {lr: 0.0001, epochs: 10, batch_size: 32, log_every: 50, save_every: 1, num_workers: 4}
